@model Restaurant.Models.Pie

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div class="col-sm-4 col-md-4 col-lg-10">
    <h4>@Model.Name</h4>
    <img src="@Model.ThumNail" class="img-rounded img-responsive" alt="ThumNail" />
    <h4>@Model.LongDescription</h4>
    <button class="btn">
        <a href="/Account/Login/">Buy Now</a>
    </button>
</div>